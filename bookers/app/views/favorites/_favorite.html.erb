<% if book.favorited_by?(current_user) %>
  <p>
    <%= link_to book.favorites.count book_favorites_path(book), method: :delete, remote: true %>
  </p>
<% else %>
  <p>
    <%= link_to book.favorites.count book_favorites_path(book), method: :post, remote: true %>
  </p>
<% end %>

<!--♥ue♡sita-->